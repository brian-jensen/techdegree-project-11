import React from 'react';

// Displays message if no photos on flickr match the users text search
const NoResults = () => (
  <h3>Sorry, no photos match your search term!</h3>
);

export default NoResults;